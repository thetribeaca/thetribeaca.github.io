(function(){const stage=document.querySelector('.acad-reverence-stage');if(!stage)return;if(matchMedia('(prefers-reduced-motion: reduce)').matches){stage.style.setProperty('--unfurl','1');return}let ticking=false;function update(){const r=stage.getBoundingClientRect(),vh=innerHeight||800,travel=Math.max(1,stage.offsetHeight-vh*.45),progress=Math.min(1,Math.max(0,(vh*.62-r.top)/travel));stage.style.setProperty('--unfurl',progress.toFixed(3));ticking=false}addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(update);ticking=true}},{passive:true});addEventListener('resize',update);update()})();
